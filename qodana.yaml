version: 1.0

#Specify inspection profile for code analysis
profile:
  name: qodana.starter

#Execute command before Qodana execution (Applied in CI/CD pipeline)
bootstrap: pnpm install && pnpm prisma generate

#Specify Qodana linter for analysis (Applied in CI/CD pipeline)
linter: jetbrains/qodana-js:latest
